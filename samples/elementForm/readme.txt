root schema is elUnqualifiedNoDefaultNs.xsd, it has elementFormDefault="unqualified"
it includes elQualified.xsd which elementFormDefault="qualified"
the valid combinded.xml shows that included schema keeps it elementFormDefault - Element2 that is local, has to be quilfied
